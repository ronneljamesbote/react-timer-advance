import React from "react";
import Control from "./control";

function controlStart(props) {
  return (
    <Control {...props} style={{ background: "#28a745" }}>
      start
    </Control>
  );
}

export default controlStart;
