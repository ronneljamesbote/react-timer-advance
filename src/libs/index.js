import convertToMilliseconds from "./convertToMilliseconds";
import getTimeFormatted from "./getTimeFormatted";

export { convertToMilliseconds, getTimeFormatted };

export default { convertToMilliseconds, getTimeFormatted };
